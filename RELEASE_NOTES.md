# FileBox v0.2.0（阶段二）

阶段二发布说明

[English](RELEASE_NOTES.en.md)

## v0.2.0-v016（安全与逻辑检视修复批次）发布说明

v016 由 codex 与 DSH 双路独立检视，发现的 38 项问题已全部修复，并完成反转验证与回归测试。

### 修复清单

- **严重问题**：运行中 `admin backup` 自动执行 WAL checkpoint，restore 激活前校验数据库可读且非空；收集上传 `init/chunk` 接入磁盘保护，空间不足返回 `DISK_FULL`，并增加分片请求限速。
- **实证中等问题**：`mkdirSyncSystem` 接入只读拦截；秒传限定目标目录；分享下载先开文件再扣次，Range 在 60 秒窗口内去重；收集槽位改为 complete 成功时消耗；分享 preview 限制为文本 64KB、其他类型 512KB，并支持 Range 截断。
- **其余中等问题**：complete/rename 共享目录锁；cron 错过任务补跑；手动同步独立 context；SFTP 整体超时；批量 ZIP 总字节与磁盘检查；backup/restore 流式哈希；新增上传任务取消和同步 pull 失败回滚；同步 goroutine recover 与类型断言安全。
- **轻微问题**：完成 share_groups 清理、fileCount 口径、JWT 登出撤销、i18n 补键、batchShare 回滚、注册 5/min 限速、分片超时、审计写入去清理、TOTP 单调、只读全覆盖、秒传 fail-closed、撤销统一 404、pull 临时目录、secrets 长度校验、点击劫持头、前端 401 会话处理、restore 必需条目与九项小修，以及改密审计、唯一管理员守卫、删户清目录、不限转有限、x/crypto 升级、admin dir 前缀过滤。

### 数据与部署注意

- `backup` 现在会自动 checkpoint WAL；生产环境仍建议停服后备份，以缩小活跃写入带来的一致性窗口。
- 新增上传任务取消 API：`DELETE /api/upload-tasks/{taskID}`。取消会删除 pending 任务及临时分片并释放配额预留。
- `logout` 现在通过 `last_logout_at` 撤销旧 JWT；登出后，签发时间早于该时间的 token 不能继续访问受保护接口。

## v0.2.0-v013（修复与新功能批次）发布说明

v013 是三轮修复 + 15 项需求批次（codex 开发 + dsh 复核测试），随 v0.2.0 单二进制发布：

- **安全加固（第三轮 G1-G14）**：SFTP 指纹严格解码（拒绝 base64 padding-bit 损坏指纹，常量时间比较）；sharePreview 匿名限速；collectionUploadChunk 只读检查；覆盖上传原子替换（旧文件保留到 complete 成功）；RenameFolder 预检+失败反向修正+目录锁；限速器容量上限；backup 0600 权限/restore 解压限额；`--jwt-secret` 空值/过短校验；前端统一 401 跳登录。
- **P0 缺陷修复**：分享次数用完重复提示去重（单错误出口）；传输面板失败提示 title 悬浮完整显示；收集配额错误脱敏（`COLLECTION_QUOTA_EXCEEDED`，不泄露配额明细）；同步目录选择支持上级导航（SFTP 根 `/` + filebox 逐级上级）；传输记录 sessionStorage 持久化 + 重选文件续传。
- **P1 体验**：同步源支持选择文件；表单控件统一 40px 高度；外部上传日志带 owner/目录（token 脱敏）；收集编辑 `PUT /api/collections/{id}`（到期/次数/单文件上限）；共享顶栏组件 AuthenticatedTopbar 统一全部视图（文件/分享/同步/日志/收集/管理后台）。
- **P2 新功能**：filebox↔filebox 同步（kind/url + HTTP adapter，SSRF 防护，MVP 限制见提交说明）；批量分享聚合链接（share_groups 表 + `/g/:token` 公开页 + 单文件/选中 ZIP 匿名下载）；SQLite WAL + synchronous=NORMAL 速率优化。

## v0.2.0-v012（新功能批次）发布说明

v012 是 11 项新功能需求批次（codex 开发 + dsh 复核测试），随 v0.2.0 单二进制发布：

- **多选批量删除与统计（功能 1/2）**：文件列表复选后批量删除（`POST /api/files/batch-delete`）；管理后台用户列表展示每用户目录数、文件数、已用空间。
- **创建用户直接设置安全项（功能 3）**：新建用户弹窗直接配置 TOTP（生成一次性 secret 供转交）、下次重绑 TOTP、IP 白名单，无需二次编辑。
- **品牌布局优化（功能 4）**：品牌设置「界面主色」不再独占一行，网格布局更紧凑。
- **批量分享与下载进度（功能 5）**：`POST /api/files/batch-share` 为多选文件批量创建独立分享链接；单文件与批量 zip 下载流式显示字节/百分比/速率并支持取消。
- **用户只读时段（功能 6a）**：管理员可设一次性只读窗口，窗口内用户仅可查看/下载，写操作统一 `403 READ_ONLY`，管理员豁免。
- **外部上传收集（功能 6b）**：`/u/:token` 公开上传页，外部访客无需登录即可上传（有效期/次数/单文件大小限制），文件落入创建者目录并计入配额。
- **下载次数用完明确提示（功能 7）**：分享次数耗尽返回结构化 `403 SHARE_DOWNLOAD_LIMIT`，前端明确提示而非浏览器权限错误。
- **分享管理（功能 8）**：`/shares` 页集中管理——列表/详情、延期、增次（不降）、单条软撤销、复制链接、下载日志。
- **分享下载日志与失败原因（功能 9）**：分享者可见自己分享的下载日志（`share_owner_id`）；失败原因细分 `share_not_found/expired/revoked/limit/denied`。
- **公网 XFF 信任开关（功能 10）**：`trustProxy` 设置默认关闭，仅开启且直连 IP 在 `--trusted-proxies` 白名单内才解析 `X-Forwarded-For`。
- **同步功能（功能 11）**：按用户配置目标系统（SFTP 密码/密钥含 passphrase，AES-GCM 加密）与同步任务——push/pull 双向、自动建目录、冲突三策略、一次性/cron 周期、详细日志（30 天保留）；已在真实 Linux SFTP 服务器端到端验证。

## v0.1.1（v011 验证反馈修复）发布说明

v011 是**独立于阶段二功能开发**的验证反馈修复批次（来源：本地验证环境用户验收反馈，20 项，权威记录见 `docs/validation-feedback-v011.md`），随 v0.2.0（阶段二）版本一并发布，修复与增强全部并入 v0.2.0 单二进制（`filebox-v011.exe` / `filebox-windows-amd64.exe`）。

### 修复清单

- **并发同名上传卡死修复（问题 18）**：冲突弹窗改为队列处理（含 60 秒超时取消），一次拖入多个同名文件时依次弹出、互不覆盖，杜绝部分文件永久卡在「准备中」；重命名后用户可见文件名跟随序号（`xx.txt`、`xx (1).txt`、`xx (2).txt`）。
- **上传失败日志补全（问题 18）**：上传初始化与分片上传的每个拒绝分支记录审计与服务日志（原因细分：非法文件名/超限/同名冲突/磁盘不足/配额不足/任务不存在/分片大小不符等），后台日志页可直接筛选查看；上传失败/取消项保留在传输面板显示失败原因，可重试或移除。
- **配额提示优化（问题 19）**：整体配额不足时响应携带明细（`QUOTA_EXCEEDED` + 已用/配额/文件大小），界面展示「配额不足：当前已用 X / 总配额 Y，文件需 Z，超出 W，请清理空间或调整配额」；单文件超过 `--max-file-size` 时明确提示「文件超过单文件大小上限 M」，不再出现误导性「文件名或文件大小无效」。
- **目录重命名软删除冲突修复（D-S2-3）**：目录重命名级联改写文件路径时与软删除记录冲突导致 500，已修复并补单测。

### 增强清单

- **用户自定义目录（问题 6）**：移除自动年月目录层，支持中英文目录创建/重命名/删除（非空保护）、面包屑导航、上传到当前目录；`migrate-v010-paths` 一键迁移旧数据。
- **独立传输面板 + 整体速率（问题 4/20）**：顶栏「传输」按钮打开抽屉（上传/下载分组、下载流式进度）；抽屉顶部实时显示所有进行中上传的整体速率（单位自适应、滑动平均平滑）。
- **管理后台页签化（问题 14/15/16）**：左侧页签导航（概览/用户/安全/品牌/锁定/系统），`?tab=` 深链；新建/编辑用户为居中弹窗；日志保存周期迁入系统设置页签。
- **日志体验（问题 10/11）**：成功绿/失败红固定配色（不随主题色）；「系统配置」类操作 20+ 动作可筛选（三语文案）。
- **账号安全（问题 12）**：顶栏「修改密码」入口；管理员可「要求下次重新绑定 TOTP」。
- **品牌与交付（问题 13/17/7/8/9）**：版权信息设置与页脚品牌信息块（标题+描述+版权/ICP/公安）；顶栏 logo 点击跳首页；单文件交付（`make release`/`release.ps1` + SHA256 校验和）；README 部署指南（Windows/Linux）。
- **登录与上传体验（问题 1/2/3/5）**：登录页移除默认账号提示；新建用户弹窗化且错误内联；拖拽目录明确提示而非误报网络错误；md5 列显示开关（持久化）。

### 数据与部署

- 数据目录结构变更（移除 `yy/mm` 层）：升级部署前先运行 `filebox-v011.exe admin migrate-v010-paths --data=<data-dir>`（自动备份 DB、迁移物理目录与路径记录，幂等）。
- 二进制命名 `filebox-v011.exe`，不与 `filebox.exe`（中间版）混淆；`--data` 目录内的用户数据（品牌/文件/日志/锁定参数）在迁移后完整保留。

## 版本概览

`v0.2.0` 是 FileBox 阶段二版本，在 v0.1.0（阶段一）基础上补齐传输与协作能力，仍为 Windows/Linux 单二进制部署。

## 本次交付（阶段二）

### 分片断点续传

- 支持 2–8 MiB 分片；分片可乱序、重复上传（幂等覆盖），每片记录 SHA-256。
- 服务端以 `chunks` 表持久化已上传分片，`GET /api/files/{taskID}/status` 返回缺片清单，服务重启后仍可续传。
- `complete` 校验分片齐备后流式合并并计算 MD5/SHA-256，原子落盘；缺片拒绝提交。
- 前端 4 路并发分片、整体进度、暂停/继续（仅补传缺失分片）、失败重试（指数退避）；小文件仍走单分片直传，与阶段一完全兼容。

### 秒传

- `POST /api/files/check`：md5 优先、sha256 兜底，同一用户范围内命中即返回已有记录，不重复落盘。
- 前端用 WebCrypto 计算 sha256，上传前自动秒传检查。

### 文件夹 / 多文件上传

- `upload-init` 支持相对目录 `dir`，落盘保留 `data/files/<user>/<yy>/<mm>/<相对目录>/<name>` 结构。
- 不同相对目录下的同名文件各自独立、不加序号；目录名严格校验（拒绝 `..`、控制字符与 Windows 非法字符）。

### 分享链接

- 创建分享（有效期小时数、最大下载次数，0=不限），生成 64 位随机 token。
- 匿名 `meta` 与 `download` 接口：过期返回 404/403「分享链接已过期」，超次数返回 403「分享次数已用完」；下载次数原子计数。
- 可撤销全部分享；分享创建/查看/下载计入审计日志（`share` / `share_view` / `share_download`）。
- 前端新增 `/:token` 匿名分享页（元数据、预览、下载）。

### 在线预览

- `GET /api/files/{id}/preview`：图片/文本/视频/PDF/JSON 白名单内 `inline` 输出（支持 Range），其余类型强制 `attachment` 下载。
- 前端文件行「预览」弹窗（图片/视频/PDF/文本内嵌展示）。

### 上传限速

- 按用户令牌桶（`golang.org/x/time/rate`），管理员可配置 `uploadRateLimit`（字节/秒，`0`=不限，默认不限），作用于分片写入前。
- 管理后台以 KB/s 输入，保存后下一请求生效。

### 注册开关

- `POST /api/auth/register` 受 `registerEnabled` 设置控制（默认关闭；`--register-enabled` 仅作首次部署种子）。
- 开启后按密码策略创建普通用户并直接签发登录令牌；`GET /api/brand` 公开返回开关状态，登录页按开关显示注册入口。

### 系统统计补全

- 管理后台统计增加分享链接数（未过期）与分享累计下载次数，与用户数/文件数/用量/磁盘信息并列展示。

### 用户实测反馈批次（随 v0.2.0 一并交付）

- 登录页不再展示「首次启动默认管理员：admin / admin123」提示（三语文案一并移除）。
- 顶栏「传输」按钮展开独立传输面板，区分上传与下载；下载新增流式进度显示。
- 文件列表「完整性」列默认直接显示 md5，工具栏提供「显示 MD5」开关（选择持久化，默认显示）。
- 「上传文件夹」按钮与拖拽目录均支持文件夹上传；空目录等无效拖拽给出明确中文提示，不再误报「网络连接失败」。

### 界面直接创建目录（v011 反馈批次，随 v0.2.0 一并交付）
- 移除自动年月目录层：存储结构改为 `data/files/<user_id>/[<自定义目录>/]<文件名>`，不建目录时文件直接放用户根目录。
- 新增 `folders` 表与目录 CRUD API（创建/列表/重命名/删除，重命名级联更新子目录与文件路径并物理移动目录；非空目录禁止删除）。
- 文件页新增面包屑导航、「新建文件夹」按钮（支持中文目录名）、目录内上传（上传目标=当前目录）、列表按目录过滤。
- 用户间目录隔离；配额仍按用户维度（目录内文件计入配额）。
- 新增 `filebox admin migrate-v010-paths` 迁移命令：将旧 `files/<uid>/<yy>/<mm>` 结构迁移为 `files/<uid>/<yy>-<mm>`（如 `2026-08`），迁移前自动备份 DB、迁移后登记历史目录记录，幂等可重复执行。

### 日志与品牌体验（v011 反馈批次，随 v0.2.0 一并交付）

- 日志页「结果」列成功固定绿色、失败固定红色，不随主题色变化。
- 日志页筛选新增「系统配置」分组（配置修改/品牌/语言/密码/用户/TOTP/IP 白名单/目录/统计等 20+ 动作，三语文案齐全）。
- 顶栏新增「修改密码」入口，普通用户可随时自助改密；管理员编辑用户可勾选「要求下次重新绑定 TOTP」，用户下次登录强制重新扫码绑定。
- 品牌设置新增「版权信息」（如 `Copyright © 2026 xxx`），页脚按空值规则渲染（清空不残留空白）。

### 管理后台页签化与体验（v011 反馈批次，随 v0.2.0 一并交付）

- 管理后台重构为左侧页签导航 + 右侧内容区：概览（统计 + 系统默认语言）/ 用户管理（搜索 + 表格）/ 安全设置（密码策略 + IP 锁定）/ 品牌设置 / 锁定管理 / 系统设置（日志保存天数 + 注册开关 + 上传限速）；`?tab=` 深链直达指定页签，刷新后保持。
- 新建/编辑用户改为居中弹窗（遮罩点击关闭），角色、配额、重置密码、禁用、TOTP 重新绑定、IP 白名单等字段齐全。
- 日志页不再内嵌「日志留存周期」面板，统一到管理后台「系统设置」页签管理（接口不变）。
- 页脚升级为品牌信息块：首行站点标题 + 描述，随后版权 / ICP / 公安备案。

### 并发上传与失败提示（v011 反馈批次，随 v0.2.0 一并交付）

- 同名冲突改为队列处理：一次拖入多个同名文件时冲突弹窗依次弹出，每个文件最终必定得到处理（含 60 秒超时取消保护），不再互相覆盖导致部分文件卡在「准备中」；重命名后用户可见文件名跟随序号（`xx.txt`、`xx (1).txt`、`xx (2).txt`）。
- 上传失败/取消项保留在传输面板并显示红色失败状态与原因，可重试或移除，不再 2 秒后静默消失。
- 上传初始化与分片上传的每个拒绝分支均记录审计与服务日志（原因细分：非法文件名/超限/同名冲突/磁盘不足/配额不足/任务不存在等），后台日志页可直接筛选查看。

### 配额与单文件超限提示优化（v011 反馈批次，随 v0.2.0 一并交付）

- 整体配额不足时响应携带明细（`QUOTA_EXCEEDED` + 已用/配额/文件大小），界面展示「配额不足：当前已用 X / 总配额 Y，文件需 Z，超出 W，请清理空间或调整配额」。
- 单文件超过 `--max-file-size` 时返回 `413 FILE_TOO_LARGE`（含上限值），界面明确提示「文件超过单文件大小上限 M」，不再出现误导性「文件名或文件大小无效」。

### 整体传输速率（v011 反馈批次，随 v0.2.0 一并交付）

- 传输侧边栏顶部显示「整体速率」（如 `12.5 MB/s`），为所有进行中上传的合计速率；单位自适应（B/KB/MB/GB per s），1 秒采样 + 3 点滑动平均平滑；无进行中传输时隐藏。

### 单文件交付与部署指南

- 交付物为单个可执行文件（前端已嵌入、SQLite 纯 Go、零外部运行时依赖）；`make release`（或 Windows 下 `scripts\release.ps1`）产出 `dist/filebox-windows-amd64.exe`、`dist/filebox-linux-amd64` 与 `dist/SHA256SUMS.txt`（CGO_ENABLED=0、-trimpath、-s -w 静态精简），复制即用。
- README 新增「部署指南（单文件交付）」章节：Windows（下载/运行/防火墙/sc 或 NSSM/HTTPS）与 Linux（下载/chmod/前台运行/systemd 单元/Nginx 反代/--trusted-proxies）分步说明 + 生产通用注意事项。

### 阶段一功能保持

登录/登出、文件 CRUD、配额、双哈希、磁盘保护、冲突覆盖/重命名、非法文件名校验、审计日志、登录锁定、TOTP、IP 白名单、品牌、多语言、主题色、强制改密、运维命令、服务日志、服务化部署与反代配置均保持可用，并通过回归测试。

## 安全提示

- 首次启动默认管理员为 `admin/admin123`，首次登录后请立即修改密码或禁用账户。
- 公网部署必须设置强随机 `--jwt-secret` 或 `FILEBOX_JWT_SECRET`，并使用 HTTPS 反向代理。
- 分享链接可匿名下载：token 随机且不可枚举，但 `meta` 公开文件名/大小，请勿分享名称敏感的私密文件；分享可随时撤销。
- 开放注册默认关闭；如需公网开放，请确认密码策略与配额配置后由管理员在后台开启。
- 默认 JWT 有效期为 7 天；登出不建立服务端 JWT 黑名单。

## 部署与运行

开发模式依次运行：

```powershell
npm --prefix web install
npm --prefix web run build
go run ./scripts/sync-web.go
go run ./cmd/filebox
```

生产构建使用 `make build`（Windows `bin/filebox.exe`）或 `make build-linux`（Linux）；默认数据根目录 `./data`，默认监听地址 `:8080`。首次启动创建 `admin/admin123`。

## 已知限制

- 秒传仅限同一用户范围（跨用户不做内容去重）。
- 限速为每用户令牌桶，作用于分片写入；速率变更在下一请求生效。
- 未实现：废弃上传任务的定时清理、服务端主动推送上传进度。

## 变更历史

- v0.2.0 变更批次与缺陷修复记录见 [`docs/requirements/CHANGELOG.md`](docs/requirements/CHANGELOG.md)（Stage 2 批次 A/B/C + D-S2-1/D-S2-2 修复）。
- v0.1.0 阶段一交付见仓库历史与 [`docs/requirements/CHANGELOG.md`](docs/requirements/CHANGELOG.md)。
